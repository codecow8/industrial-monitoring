# 02: 发布版本闭环

**What to build:** 组态工程师可以把当前草稿发布成不可变版本；现场操作员打开运行态时读取已发布版本，后续草稿修改不会影响正在运行的页面，直到再次发布。

**Blocked by:** 01: 草稿持久化闭环.

**Status:** resolved

- [x] 发布操作为当前草稿创建新的、不可变的页面版本
- [x] 运行态只读取已发布版本，不直接读取正在编辑的草稿
- [x] 发布后继续修改草稿，运行态内容保持不变
- [x] 再次发布后，运行态读取新版本且旧版本仍可追溯
- [x] 接口测试和编辑—发布—运行态浏览器测试证明上述行为

## Comments

- 只有 01 完成后才能开始。
- 2026-09-14：已确认 HTTP 与浏览器 seam，开始发布版本 TDD。
- 2026-09-14：后端发布/追溯与浏览器 A→B 隔离完成红—绿验证，任务已解决。
